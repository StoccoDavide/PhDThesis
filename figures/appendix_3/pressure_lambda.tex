\begin{tikzpicture}

\begin{axis}[
  minor tick num=1,
  minor grid style={dashed, line width=.1pt, draw=gray!45},
  major grid style={line width=.2pt, draw=gray!60},
  width=1.5in,
  height=1.5in,
  at={(0.0in,0.0in)},
  scale only axis,
  xmax=1.1,
  xmin=-0.1,
  xlabel style={font=\color{black}},
  xlabel={$\chi$ (--)},
  ymin=-0.15,
  ymax=1.65,
  ylabel style={font=\color{black}},
  ylabel={$\Upsilon$ (--)},
  axis background/.style={fill=none},
  xmajorgrids,
  ymajorgrids,
  xminorgrids,
  yminorgrids,
  samples=50
]

\addplot[mycolor1, line width=0.9pt, domain=0.0:1.0] (x,
  0
  - x*1/3*8 %lambda
  + x*10
  + x*60*0 %delta
  + x*x*2*8 %lambda
  - x*x*30
  - x*x*180*0 %delta
  - x*x*x*10/3*8 %lambda
  + x*x*x*40
  + x*x*x*120*0 %delta
  + x*x*x*x*5/3*8 %lambda
  - x*x*x*x*20
);

\addplot[mycolor2, line width=0.9pt, domain=0.0:1.0] (x,
  0
  - x*1/3*4 %lambda
  + x*10
  + x*60*0 %delta
  + x*x*2*4 %lambda
  - x*x*30
  - x*x*180*0 %delta
  - x*x*x*10/3*4 %lambda
  + x*x*x*40
  + x*x*x*120*0 %delta
  + x*x*x*x*5/3*4 %lambda
  - x*x*x*x*20
);

\addplot[mycolor3, line width=0.9pt, domain=0.0:1.0] (x,
  0
  - x*1/3*0 %lambda
  + x*10
  + x*60*0 %delta
  + x*x*2*0 %lambda
  - x*x*30
  - x*x*180*0 %delta
  - x*x*x*10/3*0 %lambda
  + x*x*x*40
  + x*x*x*120*0 %delta
  + x*x*x*x*5/3*0 %lambda
  - x*x*x*x*20
);

\addplot[mycolor4, line width=0.9pt, domain=0.0:1.0] (x,
  0
  + x*1/3*4 %lambda
  + x*10
  + x*60*0 %delta
  - x*x*2*4 %lambda
  - x*x*30
  - x*x*180*0 %delta
  + x*x*x*10/3*4 %lambda
  + x*x*x*40
  + x*x*x*120*0 %delta
  - x*x*x*x*5/3*4 %lambda
  - x*x*x*x*20
);

\addplot[mycolor5, line width=0.9pt, domain=0.0:1.0] (x,
  0
  + x*1/3*8 %lambda
  + x*10
  + x*60*0 %delta
  - x*x*2*8 %lambda
  - x*x*30
  - x*x*180*0 %delta
  + x*x*x*10/3*8 %lambda
  + x*x*x*40
  + x*x*x*120*0 %delta
  - x*x*x*x*5/3*8 %lambda
  - x*x*x*x*20
  );
\end{axis}

\end{tikzpicture}%